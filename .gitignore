.vercel
.env.local
node_modules/

# Blog build output — regenerated by `npm run build` (anchored to root
# so `content/blog/` source markdown is still tracked)
/blog/
/sitemap.xml
/robots.txt
/llms.txt
