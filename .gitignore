.vercel
.env.local
node_modules/

# Build output — regenerated by `npm run build` (anchored to root so
# `content/blog/` source markdown is still tracked)
/blog/
/author/
/privacy/
/terms/
/sitemap.xml
/robots.txt
/llms.txt

# macOS
.DS_Store

# Source images folder (outside repo intent)
Afuera blogs/
