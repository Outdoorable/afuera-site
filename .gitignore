.vercel
.env.local
node_modules/

# Build output — regenerated by `npm run build` (anchored to root so
# `content/blog/` source markdown is still tracked)
/blog/
/privacy/
/terms/
/sitemap.xml
/robots.txt
/llms.txt
